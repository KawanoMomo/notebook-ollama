.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
dist/
!apps/web/dist/.gitkeep
build/
*.egg-info/

# Project runtime data
.notebook-ollama/
qdrant/
*.db
*.db-journal

# Visual verification scratch dir (seed script + screenshots, never committed)
.visual-verify-data/

# Dev server scratch log (ad-hoc uvicorn runs during verification)
.uvicorn-eval.log

# Editor
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# uv
.venv/

# Recording: STT/diarization models + recorded audio (fetched/reused, never committed)
models/
data/*
# Static seed files that ship with the repo (お知らせ等)
!data/notices.json
*.wav
*.m4a
*.opus
*.mp3

# Obsidian vault (settings are shared; UI state and plugin binaries are not)
.obsidian/workspace.json
.obsidian/plugins/

# Agent session data
.claudian/
.superpowers/
.worktrees/

# 評価・検証の証拠(スクリーンショット等)。ローカル成果物なので追跡しない
.eval/
